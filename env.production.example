# Configurações para Produção
# Copie este arquivo e renomeie para .env.local

# Banco de dados (substitua pela sua string de conexão)
DATABASE_URL=postgresql://usuario:senha@host:porta/database

# Autenticação (gere uma chave segura)
NEXTAUTH_SECRET=sua-chave-super-secreta-32-caracteres-minimo
NEXTAUTH_URL=https://seu-dominio.vercel.app

# Ambiente
NODE_ENV=production

# Configurações opcionais
NEXT_PUBLIC_APP_NAME=Animed
NEXT_PUBLIC_APP_URL=https://seu-dominio.vercel.app
